export default class VisitorCard {
  constructor(id, visitorId, bookId, takeDate, returnDate) {
    this.id = id
    this.visitorId = visitorId
    this.bookId = bookId
    this.takeDate = takeDate
    this.returnDate = returnDate
  }
}
