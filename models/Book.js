export default class Book {
  constructor(id, title, author, year, publisher, pages, count) {
    this.id = id
    this.title = title
    this.author = author
    this.year = year
    this.publisher = publisher
    this.pages = pages
    this.count = count
  }
}
